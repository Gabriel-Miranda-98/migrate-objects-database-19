
  CREATE OR REPLACE EDITIONABLE TYPE "ARTERH"."LOG_PROCESSAMENTO" as OBJECT (
     TIPO_LOG NUMBER,
     CODIGO_LOG CHAR(4),
     DESCRICAO_LOG VARCHAR2(4000),
     DETALHE_LOG VARCHAR2(4000)
    )

